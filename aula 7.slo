início {
    inteiro: min;
    inteiro: max;
    inteiro: n;
    
    leia("minimo", min);
    leia("maximo", max);
    
    n = min;
    enquanto(n <= max)  {
        se (n % 2 == 0) {
            escreva(n);
        }
        
        n = n + 1;
    }
}
//aprendendo a usar enquanto e usando a programação para mostrar so os numeros pares entre numeros especificados.