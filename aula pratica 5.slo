início {
    real: a;
    real: b;
    real: c;
    real: delta;
    real: x1;
    real: x2;
    
    
    leia("qual o valor de a" , a);
    leia("qual o valor de b" , b);
    leia("qual o valor de c" , c);
    
    delta = pot(b,2) -4*a*c;
    
    x1 = (-b + rad(delta)) / (2 * a);
    
    x2 = (-b - rad(delta)) / (2 * a);
    
    escreva("x1 = " + x1);
    escreva("x2 = " + x2);
    
      
}